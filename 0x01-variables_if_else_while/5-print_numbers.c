#include <stdio.h>
/**
  * main - Entry point
  * a program that prints the alphabet in lowercase, followed by a 
  * Return: 0
  */
int main(void)
{
	for (int n = 0;  n < 10; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
